# {{.ProjectName}}

Bootstrapped with Loom — the fixed files (`AGENTS.md`, `.agent/skills/`, `.ci/`) came from the shared kit; this file and `docs/prd/` are project-specific.

**Primary stack:** {{.Stack}}

## Getting started
Fill this in with real setup instructions once the first feature lands.

## Working with an AI coding agent on this repo
Read `AGENTS.md` first — it's the standing contract for how agents (and humans) are expected to work here.
